apiVersion: v1
kind: Service
metadata:
  name: driftguard
  namespace: driftguard
spec:
  selector:
    app: driftguard
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
  type: NodePort
